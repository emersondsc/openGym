# BACKLOG-05 — solução — mudou de casa

Este documento agora vive em **`backlogs/backlog05_solucao.md`** no repositório
**`emersondsc/opengym-backlog`** (privado), junto com o backlog do app.

**Não escreva aqui:** este arquivo é só um ponteiro. O histórico continua neste repositório
(`git log -- docs/backlog05_solucao.md`).
