# Backlog do openGym — mudou de casa

Este documento agora vive no repositório **`emersondsc/opengym-backlog`** (privado), em
**`backlogs/openGym.md`**, junto com o backlog de infraestrutura e as notas de operação.

**Por quê:** o backlog estava dentro do clone do app, então só existia onde o clone existia. Quando
o app migrou do Pi para a VPS, o clone do Pi foi esvaziado e o backlog deixou de existir lá; no
Windows nunca existiu. Agora tem uma fonte única, clonada nas três máquinas.

- **Onde ler:** `backlogs/openGym.md` no repo `opengym-backlog`
- **Histórico deste arquivo:** continua no histórico deste repositório
  (`git log -- docs/backlog.md`)
- **Não escreva aqui.** Edite o repositório do backlog; este arquivo é só um ponteiro.
